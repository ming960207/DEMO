# 🚀 快速开始指南

## 欢迎使用Web应用项目！

本指南将帮助您在5分钟内启动并运行整个开发环境。

## 📋 环境检查

在开始之前，请确保您的系统已安装以下软件：

```bash
# 检查Node.js版本 (需要 v18.0.0+)
node --version

# 检查npm版本 (需要 v9.0.0+)
npm --version

# 检查Git版本
git --version
```

如果版本不符合要求，请访问 [Node.js官网](https://nodejs.org/) 下载最新版本。

## ⚡ 三步启动

### 第1步: 安装依赖
```bash
# 安装所有项目依赖 (前端 + 后端)
npm run install:all
```

### 第2步: 配置环境
```bash
# 复制环境变量模板
cp .env.example .env

# 使用您喜欢的编辑器编辑环境变量
# 例如: code .env 或 vim .env
```

**重要的环境变量配置:**
```env
# 数据库连接 (如果使用本地MongoDB)
DATABASE_URL=mongodb://localhost:27017/webapp

# JWT密钥 (请更改为您自己的密钥)
JWT_SECRET=your-super-secret-jwt-key-change-this

# 应用端口 (默认配置通常无需修改)
FRONTEND_PORT=3000
BACKEND_PORT=3001
```

### 第3步: 启动开发服务器
```bash
# 同时启动前端和后端开发服务器
npm run dev
```

## 🎉 成功启动！

如果一切正常，您将看到以下输出：
```
🚀 Server running on port 3001
📊 Environment: development
🔗 API URL: http://localhost:3001/api

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

现在您可以访问：
- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:3001/api

## 🛠️ 常用开发命令

### 开发相关
```bash
# 启动开发服务器 (前端 + 后端)
npm run dev

# 仅启动前端
npm run dev:frontend

# 仅启动后端
npm run dev:backend
```

### 代码质量
```bash
# 运行所有测试
npm test

# 代码检查
npm run lint

# 代码格式化
npm run format
```

### 构建相关
```bash
# 构建生产版本
npm run build

# 清理所有依赖和构建文件
npm run clean
```

## 📁 项目结构快览

```
web-application-project/
├── frontend/           # React前端应用
├── backend/            # Node.js后端API
├── shared/             # 共享代码
├── docs/               # 项目文档
├── tests/              # 测试文件
├── .env.example        # 环境变量模板
├── package.json        # 根目录脚本
└── 项目说明.md         # 详细项目说明
```

## 🔧 故障排除

### 常见问题及解决方案

#### 1. 端口被占用
```bash
# 错误信息: EADDRINUSE: address already in use :::3000
# 解决方案: 修改 .env 文件中的端口号
FRONTEND_PORT=3001
BACKEND_PORT=3002
```

#### 2. 依赖安装失败
```bash
# 清理并重新安装
npm run clean
npm run install:all
```

#### 3. 数据库连接失败
```bash
# 确保MongoDB正在运行 (如果使用本地数据库)
# macOS/Linux:
brew services start mongodb-community
# 或
sudo systemctl start mongod

# Windows:
net start MongoDB
```

#### 4. TypeScript编译错误
```bash
# 检查TypeScript配置
npm run type-check

# 如果有类型错误，查看具体错误信息并修复
```

## 📚 下一步学习

1. **阅读项目文档**:
   - [项目说明](./项目说明.md) - 完整的项目介绍
   - [开发指南](./docs/development/开发指南.md) - 详细的开发流程
   - [需求分析](./docs/requirements/ANALYSIS.md) - 技术架构说明

2. **开始开发**:
   - 查看 `frontend/src/` 目录了解前端结构
   - 查看 `backend/src/` 目录了解后端结构
   - 运行测试了解项目质量标准

3. **学习技术栈**:
   - [React官方文档](https://react.dev/)
   - [TypeScript官方文档](https://www.typescriptlang.org/)
   - [Express.js官方文档](https://expressjs.com/)
   - [MongoDB官方文档](https://docs.mongodb.com/)

## 🆘 获取帮助

如果您遇到问题：

1. **查看文档**: 首先查看项目文档目录
2. **检查日志**: 查看控制台输出的错误信息
3. **搜索问题**: 在GitHub Issues中搜索类似问题
4. **创建Issue**: 如果问题仍未解决，创建新的Issue

## 🎯 开发建议

1. **代码提交前**:
   ```bash
   npm run lint    # 检查代码质量
   npm test        # 运行测试
   npm run format  # 格式化代码
   ```

2. **功能开发流程**:
   - 创建功能分支: `git checkout -b feature/新功能`
   - 开发并测试功能
   - 提交代码: `git commit -m "feat: 新功能描述"`
   - 创建Pull Request

3. **保持代码质量**:
   - 遵循TypeScript类型安全
   - 编写单元测试
   - 使用语义化的提交信息
   - 定期更新依赖

## 🎉 开始您的开发之旅！

现在您已经成功设置了开发环境，可以开始构建您的Web应用了。祝您开发愉快！

---

**需要更多帮助？** 查看 [项目说明.md](./项目说明.md) 获取完整的项目信息。
